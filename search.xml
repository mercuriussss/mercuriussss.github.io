<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title><![CDATA[为何 Hashtable、ConcurrentHashMap 不允许键或值为空，而 HashMap 允许]]></title>
    <url>%2F2020%2F03%2F20%2Fjava-collection-1%2F</url>
    <content type="text"><![CDATA[1、底层源码实现HashMap 在添加空键时做了特殊处理，若key为空则哈希值为0，value 则是没判空过滤操作1234567public V put(K key, V value) &#123; return putVal(hash(key), key, value, false, true);&#125;static final int hash(Object key) &#123; int h; return (key == null) ? 0 : (h = key.hashCode()) ^ (h &gt;&gt;&gt; 16);&#125;Hashtable 在添加空值时会直接抛空指针异常，因为要调用 key.hashCode() 方法，所以 key 也不允许为空，不然一样会抛出空指针异常12345678910public synchronized V put(K key, V value) &#123; // Make sure the value is not null if (value == null) &#123; throw new NullPointerException(); &#125; // Makes sure the key is not already in the hashtable. Entry&lt;?,?&gt; tab[] = table; int hash = key.hashCode(); // key不能为空 //....&#125;ConcurrentHashMap 与 Hashable同理1234final V putVal(K key, V value, boolean onlyIfAbsent) &#123; if (key == null || value == null) throw new NullPointerException(); //...&#125;2、value 为不为空的限制原因Hashtable 和 ConcurrentHashMap 都是线程安全的，使用的都是安全失败机制（fail-safe），这种机制会使你此次读到的数据不一定是最新的，但它们支持多线程操作，而 HashMap 则是线程不安全的，使用的是快速失败机制（fail-fast），只支持单线程操作。fail-fast（快速失败）：在用迭代器遍历时一旦发现容器数据被修改了，立刻抛出 ConcurrentModificationException 异常，终止遍历（强一致性，不支持并发）fail-safe（安全失败）：在用迭代器遍历时会先复制原有集合内容，随后在拷贝的集合上进行遍历，所以即使中途数据被修改了也会继续遍历下去（弱一致性，支持并发）所以当键值对key不为空、value为空时，这里会有一个问题：你如何判断 map.get(key) 取出来的值为空，是因为它值本身为空，还是因为 key 不存在，所以才返回空值的？或许你会说，在操作前先使用 map.containsKey(key) 的方法进行判断，将它们分成两种情况即可，像下面这样12345if(map.containsKey(key))&#123; methodA(); // 若map不存在该key，则执行methodA()&#125;else&#123; methodB(); // 若map存在该key，则执行methodB()&#125;但！是！这种情况只适应于单线程，也就是说这三者中只适用于 HashMap ，对于 Hashtable 和ConcurrentHashMap 而言，在多线程环境下由于这个操作是非原子性的，所以 map.containsKey(key) 这个方法并不可靠。举个例子，假设线程 1 执行这段代码，判断出map存在该key，于是打算执行 methodA() ，但它的线程在执行前被阻塞了，于是线程 1 挂起，轮到线程 2 在跑，但线程 2 接下来的操作却直接把这个 key 给删掉了，所以当又轮到线程 1 执行时，线程 1 会按照 map 依然存在该 key 的错误前提下继续执行代码，这就会引发线程的安全问题了，所以 Hashtable 和 ConcurrentHashMap 都不支持存储空值。3、key 为不为空的限制原因emmmm，这个问题就很有意思了，我想了半天也想不出为什么，直到我在网上找到一篇文章，写到了早在 2006 年就有人在网上发出求助邮件、里面就有提到这个问题，而回答这封邮件的人里就有 JUC 包的作者 Doug Lea ，以及 HashMap 的作者之一 Josh Bloch，两个巨佬的答复中直接给出了答案：他们乐意Doug Lea 讨厌 null 值，他觉得允许 null 的设计本身就是不合理的，而 Josh Bloch 也快被他说服了，觉得这或许是个错误，但他自己也不是很确定。Josh Bloch：或许是我错了但我就是不想改俩人似乎讨论这个问题已经很久了……事实就是这么操蛋。顺便贴一下这篇文章出处：https://segmentfault.com/a/1190000021105716]]></content>
      <tags>
        <tag>Java</tag>
        <tag>Java集合</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[三、Java并发编程基础——线程]]></title>
    <url>%2F2020%2F03%2F20%2Fjava-concurrent-3%2F</url>
    <content type="text"><![CDATA[1、线程的概念​ 进程是指一个内存中运行的应用程序，它是操作系统资源分配的最小单位。每个进程都有自己独立的一块内存空间，而一个进程中可以启动多个线程。​ 线程是比进程更轻量级的调度执行单位，它是进程的一个执行单元。线程的引入可以把一个进程的资源分配和执行调度分开，各个线程之间既可以共享进程资源（内存地址、文件IO等），又可以独立调度。2、线程的实现模型​ Java 使用的是 1：1 线程模型，Python 的gevent使用的是 1：N 线程模型，而 Go 使用的是 N：M 线程模型。（1）内核线程实现（1：1实现）​ 内核线程（Kernel-Level Thread，KLT）就是由操作系统内核（Kernel）支持的线程，这种线程由内核来完成线程切换操作。​ 程序一般不会直接使用内核线程（那样太危险了），而是使用内核线程的一种高级接口——轻量级进程（Light Wegiht Process，LWP）来操作内核进程。轻量级进程即我们常说的线程，由于每个轻量级线程都由一个内核线程支持，也就是说 LWP 和KLT 之间是 1：1 的关系，因此也称这种模型为一对一的线程模型。​ 这类似于一种代理模式，LWP 就是代理对象，而 KLT 则是被代理对象，我们把任务请求发给代理人 LWP，然后 LWP 会通过调用真实具备执行任务能力的被代理人 KLT 去执行任务。图3-1 轻量级进程与内核线程之间1：1的关系图优点：每个 LWP 都是一个独立的调度单元，即便有一个 LWP 在系统调用中被阻塞了，也不会影响整个进程继续工作，系统的稳定性会比较好。线程的调度和各种操作都委托给了操作系统，所以实现上比较简单。缺点：各种线程操作（创建、析构、同步等）都需要进行系统调用，而系统调用的代价较高，需要在用户态和内核态中来回切换，这需要消耗掉一定时间。每个 LWP 都需要一个 KLT 支持，即每个 LWP 都会消耗掉一部分内核资源（例如内核线程的栈空间），因此系统可以支持的 LWP 数量是有限的。（2）用户线程实现（1：N实现）​ 广义上讲，一个线程只要不是 KLT ，都可以认为是用户线程（User Thread，UT）的一种。​ 狭义上讲， UT 指的是完全建立在用户空间的线程，即操作系统感知不到线程的存在，只知道那个掌控着这些 UT 的进程 P 。因此，进程和 UT 之间的比例是 1：N 。图3-2 进程与用户线程之间1：N的关系图优点：UT 的创建、同步、销毁、调度都是在用户态完成的，完全不需要切换到内核态，因此各种线程操作可以非常快速且低消耗。由于进程和 UT 之间的比例为 1：N，所以可以支持更大规模的 UT 数量，部分高性能数据库中的多线程就是由 UT 实现的。缺点：由于没有系统内核的支持，所以所有的线程操作都需要程序自己实现，这就使得 UT 的实现程序通常都比较复杂，甚至有些是不可能实现的。现在使用 UT 的程序越来越少了，Java 和 Ruby 等语言都曾使用过 UT ，但最终又放弃了，而 Golang 、 Erlang 等以高并发为卖点的新语言则普遍支持了 UT 。（3）用户线程加轻量级进程混合实现（N：M实现）​ 这种混合模式下，既存在 UT ，也存在 KLT ，被称之为 N：M 实现。图3-3 用户线程与轻量级进程之间N：M的关系图该实现模型有以下特点：UT 还是完全建立在用户空间中，因此线程的创建、切换、析构等消耗依旧很小，同时也可以支持大规模的 UT 并发。对于线程的调度，则使用 LWP 作为 UT 和 KLT 之间的桥梁，这样可以使用操作系统提供的线程调度功能和处理器映射了。UT 的系统调用要通过 LWP 来完成，大大降低了整个进程被完全阻塞的风险。UT 和 LWP 之间的数量比是不定的，即两者数量是 N：M 的关系。许多UNIX系列的操作系统，如Solaris、HP-UX等都提供了M：N的线程模型实现3、线程的调度​ 线程调度是指系统为线程分配处理器使用权的过程，调度主要方式有两种，分别是协同式（Cooperative Threads-Scheduling）线程调度和抢占式（Preemptive Threads-Scheduling）线程调度。协同式线程调度： 线程的执行时间由线程本身来控制，线程执行完自己的任务之后，主动通知系统切换到另一个线程。优点： 实现简单，切换操作对于线程自己可知，一般没有线程同步的问题。缺点： 线程执行时间不可控，如果一个线程编写有问题而一直不告知系统进行线程切换，程序会一直阻塞在那里。抢占式线程调度： 每个线程由系统分配执行时间，线程的切换不由程序本身来决定，而是由系统决定。优点： 线程执行时间可控，不会因一个线程出错而耽误整个进程乃至系统。缺点： 存在线程同步的问题，并且线程切换控制比较复杂。Java 使用的线程调度方式就是这种。4、线程的生命周期状态（1）通用的线程生命周期​ 通用的线程生命周期模型主要将线程的状态分为以下五种：初始：线程从创建到被cpu执行之前的这个阶段。这个状态下的线程仅仅是在编程语言层面被创建，而在操作系统层面并没有被创建，因此还不能被分配CPU资源，这相当于Java中new了个Thread对象但还没调用 start() 方法。就绪：指线程可以分配cpu执行。在这种状态下，真正的操作系统线程已经被成功创建了，所以可以分配 CPU 执行。运行：表示线程正获得cpu在运行。当有空闲的CPU资源时，操作系统会将资源分配给处于就绪状态的线程，这时线程的状态就将转为运行状态阻塞：指线程在执行中因某件事而受阻，处于暂停执行的状态，并且放弃自己的CPU使用权。当它的阻塞状态结束了，它的状态会变为就绪状态，等待再次被分配 CPU 资源。终止：当线程执行完或出现异常时，它就会进入终止状态，不会再切换到其他任何状态，这也意味着线程的生命周期结束了。图3-4 通用的线程生命周期图（2）Java 的线程生命周期​ Java语言中线程共有六种状态：新建（New）：创建后尚未启动的线程处于这种状态。运行（Runnable）：包含操作系统线程状态中的就绪和运行状态，处于该状态的线程可能正在执行，也可能在等待着操作系统为它分配CPU资源。等待（Waiting）：处于这种状态的线程不会被分配CPU资源，它们要等待被其他线程显式唤醒。超时等待（Timed Waiting）：处于这种状态的线程不会被分配CPU资源，不过在一定时间之后，即使不被其他线程显示唤醒，也会由操作系统自动唤醒。阻塞（Blocked）：线程被阻塞了，“阻塞状态”与“等待状态”的区别是“阻塞状态”在等待着获取到一个排它锁，这个事件将在另外一个线程放弃这个锁的时候发生；而“等待状态”则是在等待一段时间，或者唤醒动作的发生。结束（Terminated）：已终止线程的线程状态，线程已经结束执行。图3-5 Java线程的生命周期图5、Java 多线程的实现方式（1）继承 Thread 类​ Java 中提供了一个 java.lang.Thread 的程序类，底层是继承了Runnable接口的实现类。一个类只需要继承此类就表示此类为线程的主体，再覆写一个run()方法，就可以使用start()方法启动线程了。​ 选择继承Thread类实现多线程的缺点是扩展性差，因为Java程序只允许单继承一个类。12345678910class MyThread extends Thread &#123;//线程主体类 private String name; public MyThread(String name) &#123; this.name = name; &#125; @Override public void run() &#123;//线程的主体方法 System.out.println("test: " + this.name); &#125;&#125;​ 任何情况下，只要定义了多线程，那么多线程的启动永远只有一种方法，即Thread类的start()方法。（2）实现 Runnable 接口​ 一般情况下多线程多采用实现java.lang.Runnable接口的方式实现，因为这样做不会有单继承的局限性，扩展性更佳，不过启动时还是需要通过Thread类实现。12345678910111213141516171819class MyThread implements Runnable&#123;//线程主体类 private String name; public MyThread(String name) &#123; this.name = name; &#125; @Override public void run() &#123;//线程的主体方法 System.out.println("test: " + this.name); &#125;&#125;public class ThreadDemo &#123; public static void main(String[] args) &#123; Thread threadA = new Thread(new MyThread("线程A")); Thread threadB = new Thread(new MyThread("线程B")); threadA.start(); threadB.start(); &#125;&#125;（3）实现 Callable 接口​ 如果当线程执行完毕时，需要获取它的返回值，那么就可以采用Callable接口实现多线程（Runnable接口的线程无返回值）。123456Callable接口的源码@FunctionalInterfacepublic interface Callable&lt;V&gt; &#123; public V call() throws Exception;&#125;​ Callbale定义的时候可以设置一个泛型，此泛型的类型就是返回数据的类型，这样的的好处是可以避免向下转行所带来的安全隐患。12345678910111213141516class MyThread implements Callable&lt;String&gt; &#123; @Override public String call() throws Exception &#123; for ( int x = 0 ; x &lt; 10 ; x ++ ) &#123; System.out.println("thread work，x = " + x); &#125; return "finished"; &#125;&#125;public class demo &#123; public static void main(String[] args) throws Exception&#123; FutureTask futureTask = new FutureTask(new MyThread()); new Thread(futureTask).start(); System.out.println("thread return：" + futureTask.get()); &#125;&#125;Runnable 与 Callable的区别：Runnable是在JDK1.0的时候提出的多线程的实现接口，而Callable是在JDK1.5之后提出的；java.lang.Runnable 接口之中只提供了一个run（）方法，并且没有返回值；java.util.concurrent.Callable接口提供有call()，可以有返回值；6、启动与终止线程的方法​ 启动线程是调用线程类的start()方法即可，但终止线程不能使用stop()方法，因为它在终止线程时不会保证占用得线程资源会被正常释放。​ suspend()方法是暂停线程，线程进入休眠时也不会释放被占用的资源。​ 比较安全地终止线程的方法是设置控制信号变量，或者使用 interrup() 方法123456789101112131415161718192021222324252627282930public class Shutdown &#123; public static void main(String[] args) throws Exception &#123; Runner one = new Runner(); Thread countThread = new Thread(one, "CountThread"); countThread.start(); // 睡眠1秒，main线程对CountThread进行中断，使CountThread能够感知中断而结束 TimeUnit.SECONDS.sleep(1); countThread.interrupt(); Runner two = new Runner(); countThread = new Thread(two, "CountThread"); countThread.start(); // 睡眠1秒，main线程对Runner two进行取消，使CountThread能够感知on为false而结束 TimeUnit.SECONDS.sleep(1); two.cancel(); &#125; private static class Runner implements Runnable &#123; private long i; private volatile boolean on = true; @Override public void run() &#123; while (on &amp;&amp; !Thread.currentThread().isInterrupted())&#123; i++; &#125; System.out.println("Count i = " + i); &#125; public void cancel() &#123; on = false; &#125; &#125;&#125;]]></content>
      <tags>
        <tag>Java</tag>
        <tag>并发</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[二、Java并发编程基础——Java内存模型（JMM）]]></title>
    <url>%2F2020%2F03%2F17%2Fjava-concurrent-2%2F</url>
    <content type="text"><![CDATA[1、硬件的利用效率与一致性​ 由于计算机的存储设备与处理器的运算速度相差好几个数量级，CPU经常需要等待主存，白白浪费资源，所以现代计算机加入一层或多层读写速度尽可能接近处理器运算速度的高速缓存（Cache）来作为内存与处理器之间的缓冲 ( 结构 : cpu -&gt; cache -&gt; memory ) 。​ 这种CPU多级缓存的方式很好地解决了CPU与内存速度之间的矛盾，但也引入了一个新的问题：缓存一致性（Cache Coherence）。为了解决一致性问题，各个处理器在访问缓存以及读写时都需要遵循一些协议，这类协议有MSI、MESI（Illinois Protocol）、MOSI、Synapse、Firefly及Dragon Protocol等。​ 除了增加高速缓存外，为了提高硬件的利用率，处理器还可能会对输入代码进行乱序执行（Out-Of-Order Execution）优化，即保证最终结果与顺序执行的一致，但不保证程序中每个语句计算的先后顺序，类似于 JVM 中的指令重排序优化。2、主内存与工作内存从变量、主内存、工作内存的定义来看：主内存主要对应于Java堆中的对象实例数据部分工作内存则对应虚拟机栈中的部分区域从更基础的层次上说：主内存直接对应于物理硬件的内存为了获取更好的运行速度，虚拟机（或是硬件、操作系统本身的优化措施）可能会让工作内存优先存储于寄存器和高速缓存中，因为程序运行时主要访问的是工作内存。​ JMM 规定了所有的变量都存储在主内存中，每个线程都有自己的工作内存，线程的工作内存中保存的是当前线程使用到的变量值的副本（从主内存拷贝过来的）。​ 线程对变量的所有操作都必须在工作内存中进行，不能直接读写主内存中的数据，线程之间的相互传值也需要通过主内存来完成。图2-1 线程、主内存、工作内存三者的交互关系3、八种原子操作与执行规则（1）八种原子操作Java内存模型定义了以下 8 种操作来完成主内存与工作内存交互的工作：lock (锁定)：作用于主内存的变量。把一个变量标识为一条线程独占的状态。unlock (解锁)：作用于主内存的变量.把一个处于锁定状态的变量释放出来。read (读取)：作用于主内存的变量。把一个变量值从主内存传输到线程的工作内存中，以便随后的load动作使用。load (载入)：作用于工作内存的变量，它把read操作从主内存中得到的值放入工作内存的变量副本中。use (使用)：作用与工作内存的变量.它把工作内存中一个变量值传递给执行引擎，每当虚拟机遇到一个需要使用到变量的值的字节码指令时就会执行这个操作。assign (赋值)：作用于工作内存的变量，它把一个从执行引擎收到的赋值给工作内存的变量，每当虚拟机遇到一个给变量赋值的字节码指令时执行这个操作。store (存储)：作用于工作内存的变量，它把工作内存中的一个变量的值传送到主内存中，以便随后的 wirte 操作使用。wirte (写入)：作用于主内存的变量，它把 store 操作从工作内存中得到的变量值放入主内存中。图2-2 同步操作与规则图（2）执行规则这 8 种原子操作的执行规则大致可以划分为两类，一类是有关变量拷贝过程的规则，另一类是有关加锁的规则。有关变量拷贝过程的规则：不允许 read 和 load，store 和 write 单独出现，即不允许一个变量从主内存读取了但工作内存不接受，或者工作内存发起回写了但主内存不接受的情况出现。不允许线程丢弃它最近的 assign 操作，即变量在工作内存中改变后必须把该变化同步回主内存中。不允许线程无原因地（没有发生过任何 assign 操作）将数据从工作内存同步回主内存中，也就是说，只有虚拟机遇到变量赋值的字节码时才会将工作内存同步回主内存。新的变量只能从主内存中诞生，即不能在工作内存中使用未被初始化（load 或 assign）的变量，一个变量在 use 和 store 前必须先经过 load 和 assign 操作。有关加锁的规则：一个变量在同一时刻只允许一个线程对其进行 lock 操作，但是 lock 操作可以被同一个线程多次执行（锁的可重入），多次执行后只有执行相同次数的 unlock 操作，变量才会被解锁。对一个变量进行 lock 操作会清空这个变量在工作内存中的值，然后在执行引擎使用这个变量时，需要通过 assign 或 load 重新对这个变量进行初始化。对一个变量执行 unlock 前，必须将该变量同步回主内存中，即执行 store 和 write 操作。一个变量没有被 lock，就不能被 unlock，也不能去 unlock一个被其他线程 lock 的变量。4、as-if-serial 语义（1）定义​ as-if-serial语义的意思是不管怎么重排序（编译器和处理器为了提高并行度），（单线程）程序的执行结果不能改变。​ 编译器、runtime和处理器都必须遵守as-if-serial语义。​ 为了遵守as-if-serial语义，编译器和处理器不会对存在数据依赖关系的操作进行重排序，因为这种重排序会改变执行结果，但反过来说，如果不存在依赖关系则可以进行重排序。（2）as-if-serial 和 Happens-Beforeas-if-serial语义保证单线程内程序的执行结果不被改变，happens-before关系保证正确同步的多线程程序的执行结果不被改变。as-if-serial语义给编写单线程程序的程序员创造了一个幻境：单线程程序是按程序的顺序来执行的。happens-before关系给编写正确同步的多线程程序的程序员创造了一个幻境：正确同步的多线程程序是按happens-before指定的顺序来执行的。as-if-serial语义和happens-before这么做的目的，都是为了在不改变程序执行结果的前提下，尽可能地提高程序执行的并行度5、Happens-Before 规则（1）由来​ 为了提高性能，编译器和处理器常常会对指令做重排序，主要包括编译器优化的重排序、指令级并行的重排序、内存系统的重排序三部分，而源代码从开始到最终执行会依次经过这三个重排序的优化。​ 为了避免编译优化对并发编程安全性的影响，从 JDK 5 开始 JMM 就使用 Happens-Before 规则来保证并发编程的安全性，要求编译器优化后需要满足 Happens-Before 规则。（2）定义​ Happens-Before，即“先行发生”规则，指的是对于不同的线程，前面的操作也应该发生在后面操作的前面，也就是说，Happens-Before 规则保证：前面的操作的结果对后面的操作一定是可见的。​ 它上是一种顺序约束规范，用来约束编译器的优化行为，即为了执行效率，我们允许编译器的优化行为，但是为了保证程序运行的正确性，我们要求编译器优化后需要满足 Happens-Before 规则。（3）具体内容​ 根据类别，我们将 Happens-Before 规则分为了以下 4 类：操作的顺序：程序顺序规则： 如果代码中操作 A 在操作 B 之前，那么同一个线程中 A 操作一定在 B 操作前执行，即在本线程内观察，所有操作都是有序的。传递性： 在同一个线程中，如果 A 先于 B ，B 先于 C 那么 A 必然先于 C。锁和 volatile：监视器锁规则： 监视器锁的解锁操作必须在同一个监视器锁的加锁操作前执行。volatile 变量规则： 对 volatile 变量的写操作必须在对该变量的读操作前执行，保证时刻读取到这个变量的最新值。线程和中断：线程启动规则： Thread#start() 方法一定先于该线程中执行的操作。线程结束规则： 线程的所有操作先于线程的终结。中断规则： 假设有线程 A，其他线程 interrupt A 的操作先于检测 A 线程是否中断的操作，即对一个线程的 interrupt() 操作和 interrupted() 等检测中断的操作同时发生，那么 interrupt() 先执行。对象生命周期相关：终结器规则： 对象的构造函数执行先于 finalize() 方法。6、volatile 的实现原理（1）简介​ volatile 是 JVM 提供的最轻量级的同步机制，它可以保证数据的 可见性，避免出现数据脏读的现象。​ volatile 变量是从工作内存中读取的，只是它有特殊的操作顺序规定，使得看起来像是直接在主内存中读写。​ 每个线程操作数据的时候会把数据从主内存读取到自己的工作内存，如果他操作了数据并且写回了，那其他已经读取的线程的变量副本就会失效了（volatile底层有标记当前值是否失效的状态），需要对数据进行操作又要再次去主内存中读取了。（2） volatile 变量的两个特点​ ①保证对所有线程的可见性​ 这里的“可见性”指的是当一个线程修改了这个变量的值，新增对于其他线程来说是可以立即得知的。普通变量的值在线程间传递时需要通过主内存来完成，比如线程 A 修改一个普通变量的值，然后向主内存进行回写，而线程 B 在线程 A 回写完成了之后再对主内存进行读取操作，这时新变量值才会对线程 B 可见。​ 可见性的保证是基于CPU的内存屏障指令，被JSR-133抽象为happens-before原则。​ ②禁止指令重排序优化​ 普通变量仅会保证在该方法的执行过程中，所有依赖赋值结果的地方都能获取到正确的结果，而不能保证变量赋值操作的顺序与程序代码中的执行顺序一致。​ 编译时JVM编译器遵循内存屏障的约束，运行时依靠CPU屏障指令来阻止重排。图2-3 volatile读写的指令图（3） 实现Happens-Before规则的要求​ Happens-Before 原则的规定保证了操作间的可见性，volatile 变量保证了有序性和可见性，volatile 的特性得以于Java语言中的 Happens-Before 规则。​ Happens-Before 规则中要求，对 volatile 变量的写操作必须在对该变量的读操作前执行，具体实现方法分两步：① 保证动作发生​ 首先，在对 volatile 变量进行读取和写入操作，必须去主内存拉取最新值，或是将最新值更新进主内存，不能只更新进工作内存而不将操作同步进主内存，即在执行 read、load、use、assign、store、write 操作时：use 操作必须与 load、read 操作同时出现，不能只 use，不 load、read。use ← load ← readassign 操作必须与 store、write 操作同时出现，不能只 assign，不 store、write。assign → store → write​ 此时，我们已经保证了将变量的最新值时刻同步进主内存的动作发生了，接下来，我们需要保证这个动作，对于不同的线程，满足 volatile 变量的 Happens-Before 规则：对变量的写操作必须在对该变量的读操作前执行。② 保证动作按正确的顺序发生​ 其实，导致这个执行顺序问题的主要原因在于，这个读写 volatile 变量的操作不是一气呵成的，它并不是原子的。无论是读还是写，它都分成了 3 个命令（use ← load ← read 或 assign → store → write），这就导致了，你能保证 assignA 发生在 useB 之前，但你根本不能保证 writeA 也发生在 useB 之前，而如果 writeA 不发生在 useB 之前，主内存中的数据就是旧的，线程 B 就读不到最新值。​ 好比一个写操作，发生在它之前的读操作可以随便执行，但是发生在它之后的读操作，必须等把 3 个命令都执行完才能执行，即 “对变量的写操作必须在对该变量的读操作前执行” 。（4）volatile 的具体实现​ Java 巧妙的利用了 lock 操作的特点，通过观察对 volatile 变量的赋值操作的反编译代码可以看出，在执行了变量赋值操作之后，额外加了一行：1lock addl $0x0,(%esp)这一句的意思是：给 ESP 寄存器 +0，这是一个无意义的空操作，重点在 lock 上：保证动作发生：lock 指令会将当前 CPU 的 Cache 写入内存，并无效化其他 CPU 的 Cache，相当于在执行了 assign 后，又进行了 store -&gt; write；这使得其他 CPU 可以立即看见 volatile 变量的修改，因为其他 CPU 在读取 volatile 变量时，会发现自己的缓存过期了，于是会去主内存中拉取最新的 volatile 变量值，也就被迫在 use 前进行一次 read -&gt; load。保证动作顺序：lock 的存在相当于一个内存屏障，使得在重排序时，不能把后面的指令排在内存屏障之前。7、final（1）写final域的重排序规则​ 写final域的重排序规则禁止把final域的写重排序到构造函数之外。​ 该规则的实现依靠编译器在final域的写之后，构造函数return之前，插入一个StoreStore屏障。这个屏障禁止处理器把final域的写重排序到构造函数之外。​ 写final域的重排序规则的作用：可以确保在对象引用为任意线程可见之前，对象的final域已经被正确初始化过了，而普通域不具备这个报障。（2）读final域的重排序规则​ 读final域的重排序规则是在一个线程中，初次读对象引用与随后初次读该对象包含的final域，JMM禁止处理器重排序这两个操作（注意该规则仅针对处理器）。​ 该规则的实现依靠编译器在读final域操作的前面插入一个LoadLoad屏障。​ 读final域的重排序规则的作用：可以确保在读一个对象的final域之前，一定会先读包含这个final域的对象的引用。如果引用不为null，那引用对象的final域一定已经被初始化了。关于 volatile 详细解析：https://www.zhihu.com/question/31990408/answer/1028941563https://zhuanlan.zhihu.com/p/137193948]]></content>
      <tags>
        <tag>Java</tag>
        <tag>并发</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[一、Java并发编程基础——预备知识]]></title>
    <url>%2F2020%2F03%2F12%2Fjava-concurrent-1%2F</url>
    <content type="text"><![CDATA[1、上下文切换（1）时间片​ 即使是单核处理器也支持多线程执行代码，CPU通过给每个线程分配CPU时间片来实现这个机制。时间片是CPU分配给每个线程的时间，由于时间片非常短，所有CPU通过不停地切换线程执行，导致我们感觉多个线程是同时执行的，时间片一般是几十毫秒（ms）。（2）上下文切换​ CPU通过时间片分配算法来循环执行任务，当前任务执行一个时间片后就会切换到下一个任务。但是，CPU在切换前会保存上一个任务的状态，以便下次切换回该任务时能够从上次执行的位置继续执行下去。这个任务从保存到再次加载的过程就是一场上下文切换。（3）减少上下文切换的方法无锁并发编程：多线程竞争锁时，会引起上下文切换，所以多线程处理数据时，可以用一些办法来避免使用锁，例如将数据的ID按照Hash算法取模分段，不同线程处理不同段的数据。CAS算法：Java的Atomic包使用CAS算法来更新数据，不需要加锁。使用最少线程：避免创建不需要的线程，比如任务很少但创建了大量线程来处理，这会导致大量线程都处于等待状态。协程：在单线程里实现多任务的调度，并在单线程里维持多个任务间的切换。2、死锁（1）含义​ 当两个或两个以上的线程在执行过程中，因为竞争资源而造成的一种僵局，线程相互持有对方所需的资源又不释放，若无外力作用，它们都将无法推进下去。（2）死锁产生的4个必要条件互斥：线程要求对所分配的资源进行排它性控制，即在一段时间内某资源仅为一线程所占用。占有和等待：当线程因请求资源而阻塞时，对已获得的资源保持不放。不可抢占：线程已获得的资源在未使用完之前，不能剥夺，只能在使用完时由自己释放。循环等待：在发生死锁时，必然存在一个线程–资源的环形链。（3）避免死锁的方法资源一次性分配：一次性分配所有资源，这样就不会再有请求了（破坏请求条件）只要有一个资源得不到分配，也不给这个进程分配其他的资源（破坏请保持条件）可剥夺资源：即当某进程获得了部分资源，但得不到其它资源，则释放已占有的资源（破坏不可剥夺条件）资源有序分配法：系统给每类资源赋予一个编号，每一个进程按编号递增的顺序请求资源，释放则相反（破坏环路等待条件）（4）Java避免死锁的常见方法避免一个线程同时获取多个锁。避免一个线程在锁内同时占用多个资源（例如ConcurrentHashMap的实现），尽量保证每个锁只占用一个资源尝试使用定时锁，使用lock.tryLock（timeout）来替代使用内部锁机制，一定时间内获取不到就返回。对于数据库锁，加锁和解锁必须在同一个数据库连接里，否则会出现解锁失败的情况。3、资源限制（1）含义​ 资源限制是指再进行并发编程时，程序的执行速度受限于计算机硬件资源或软件资源。例如，服务器的带宽只有2Mb/s，某个资源的下载速度是1Mb/s，即使系统启动10个线程下载资源，下载速度也不会编程10Mb/s。（2）资源限制会引发的问题​ 在并发编程中，若受限于资源，将代码从串行执行的部分改成并发执行，不仅不会加快执行速度，反而会因为增加了上下文切换和资源调度的时间，变得更慢。（3）软件资源​ 软件资源受限有数据库的连接数、socket连接数等等。​ 解决方法可以是考虑使用资源池将资源复用，例如使用连接池将数据库和socket连接复用，或者再调用对方webservice接口获取数据时，只建立一个连接。（4）硬件资源​ 硬件资源受限有带宽的下载和上传速度、硬盘读写速度、CPU处理速度等等。​ 解决方法可以是考虑使用集群并发执行程序。既然单击资源有限，那就让程序在多机上运行，例如使用ODPS、Hadoop或者自己搭建服务器集群，不同机器处理不同的数据。​ 上图为《Java并发编程的艺术》的思维导图，图片出处：https://github.com/zaiyunduan123/java-concurrent-art]]></content>
      <tags>
        <tag>Java</tag>
        <tag>并发</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[六、栈帧结构]]></title>
    <url>%2F2019%2F09%2F18%2Fjvm-6%2F</url>
    <content type="text"><![CDATA[1、含义JVM以方法作为最基本的执行单元，“栈帧”（Stack Frame）则是用于支持JVM进行方法调用和方法执行背后的数据结构，它也是JVM运行时数据区中的虚拟机栈的栈元素。每个方法从调用开始到执行结束的过程，都对应着一个栈帧在虚拟机栈中从入栈到出栈的过程。每一个栈帧都包括了局部变量表、操作数栈、动态连接、方法返回地址和一些额外的附加信息。图6-1 栈帧的概念结构2、局部变量表局部变量表是一组变量值的存储空间，用于存放方法参数和局部变量。在Class 文件的方法表的 Code 属性的 max_locals 指定了该方法所需局部变量表的最大容量。局部变量表的基本单位为变量槽（Variable slot），正常来说一个slot的占用32位的长度内存，可以存放 boolean、byte、char、short、int、float、reference 和 returnAddress 8种类型，而 对于64位的 long 和 double 变量而言，虚拟机会为其分配两个连续的 Slot 空间。JVM是通过索引定位的方式使用局部变量表，而索引值的范围是从0到最大的变量槽数量。当调用方法是非static 方法时，局部变量表中第0位索引的 Slot 默认是用于传递方法所属对象实例的引用（reference），即 “this” 关键字指向的对象。分配完方法参数后，便会依次分配方法内部定义的局部变量。为了节省栈帧空间，局部变量表中的 Slot 是可以复用的。因为即使是一个方法内也存在着作用域，当离开了某些变量的作用域之后，这些变量对应的 Slot 空间就可以交给其他变量使用。但是这种机制有时候会影响垃圾回收行为，原因很简单，当离开某个作用域时，如果没有新的变量值覆盖之前作用域内的变量（指reference）空间，那么当垃圾回收时，则该引用对应的java堆中的内存则不允许被回收，因为局部变量表中还存在该引用。所以问题在于虚拟机并没有主动清理局部变量表中离开作用域的变量值，而是采用新盖旧的方法被动清理。3、操作数栈操作数栈也被称为操作栈，用于存放下一个执行的字节码指令。在Class文件的Code属性的 max_stacks 指定了执行过程中的最大栈深度。操作数栈的每一个元素都可以是包含long、double在内的任意Java数据类型。32位数据类型所占的栈容量为1，64位数据类型锁占的栈容量为2。当一个方法刚开始执行时，这个方法的操作数栈是空的，在方法的执行过程中，会有各种字节码指令向操作数栈中写入和提取内容，也就是入栈出栈的操作。Java虚拟机的解释执行引擎称为“基于栈的执行引擎”，其中所指的“栈”就是操作数栈。如果当前线程请求的栈深度大于虚拟机所允许的最大深度，将抛出StackOverflowError异常。在概念模型中，两个栈帧是相互独立的。但是大多数虚拟机的实现都会进行优化，令两个栈帧出现一部分重叠。令下面的部分操作数栈与上面的局部变量表重叠在一块，这样在方法调用的时候可以共用一部分数据，无需进行额外的参数复制传递。图6-2 两个栈帧之间的数据共享4、动态连接每个栈帧都包含一个指向当前方法所在类型的运行时常量池的引用，持有这个引用是为了支持方法调用过程中的动态连接（Dynamic Linking）。Java源文件被编译到字节码文件中时，所有的变量和方法引用都作为符号引用(Symbolic Reference)保存在class文件的常量池里。Class 文件中存放了大量的符号引用，字节码中的方法调用指令就是以常量池中指向方法的符号引用作为参数。这些符号引用一部分会在类加载阶段或第一次使用时转化为直接引用，这种转化称为静态解析。另一部分将在每一次运行期间转化为直接引用，这部分称为动态连接。5、方法返回地址当一个方法开始执行后，有两种方式可以退出当前方法。当执行引擎遇到任意一个方法返回的字节码指令（return），会将返回值传递给上层的方法调用者，这种退出方式被称之为“正常调用完成”。一般来说，方法正常退出时调用者的PC计数器可以作为返回地址，栈帧中很可能会保存这个计数器值。当执行遇到异常，并且在当前方法体内没有得到处理，就会导致方法退出，此时是没有返回值的，这种方式被称之为“异常调用完成”。这种情况下，返回地址要通过异常处理器表来确定。本质上，方法的退出就是当前栈帧出栈的过程。此时，需要恢复上层方法的局部变量表和操作数栈、将返回值（如果有的话）压入调用者栈帧的操作数栈、调整PC寄存器的值以指向方法调用指令后面的一条指令等，让调用者方法继续执行下去。https://blog.csdn.net/dyangel2013/article/details/106588217]]></content>
      <tags>
        <tag>Java</tag>
        <tag>JVM</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[五、Java类加载机制]]></title>
    <url>%2F2019%2F09%2F17%2Fjvm-5%2F</url>
    <content type="text"><![CDATA[一、类的生命周期图5-1 Java类的生命周期​ 注意：解析阶段不一定按照顺序来执行，它可以在初始化阶段之后再开始，这是为了支持Java语言的运动时绑定 （也称之为动态绑定或晚期绑定）二、类的初始化有 6 种情况会触发类的初始化遇到new、getstatic、putstatic或invokestatic这4条字节码指令使用java.lang.reflect包的方法对类进行反射调用的时候当初始化一个类的时候，若发现其父类还未初始化，则需先触发父类的初始化当虚拟机启动时，用户需要指定一个要执行的主类（即包括main方法的那个类），虚拟机会先初始化这个类当使用JDK 7 的动态语言支持时，如果一个java.lang.invoke.MethodHandle实例最后的解析结果REF_getStatic、REF_putStatic、REF_invokeStatic的方法句柄，并且该方法句柄所对应的类没有进行过初始化，则需要先触发其初始化当一个接口中定义了JDK 8 新加入的默认方法（被default关键字修饰的接口方法）时，如果有该接口的实现类发生了初始化，那该接口要在其之前被初始化不会触发类初始化的情况通过子类引用父类的静态字段，只会触发父类的初始化而不会导致子类被初始化，至于会不会触发子类的加载和验证，这个要取决于虚拟机的具体实现通过数组定义来引用类，不会触发此类的初始化，例如 Test[] test = new Test[450]引用一个类的常量也不会触发类的初始化，因为常量在编译阶段会存入调用类的常量池中，本质上并没有直接引用到定义常量的类三、生命周期具体过程1、加载​ 该阶段虚拟机要完成三件事通过一个类的全定名来获取定义此类的二进制字节流（不一定要从一个Class文件中获取，也可以从ZIP包、网络、动态生成、数据库等渠道获取）将这个字节流的二进制内容加载到虚拟机的方法区在内存中生成一个java.lang.Class对象，作为方法区这个类的各种数据的访问入口&emsp;&emsp;相对于类加载的其他阶段而言，加载阶段（准确地说，是加载阶段获取类的二进制字节流的动作）是可控性最强的阶段，因为开发人员既可以使用系统提供的类加载器来完成加载，也可以自定义自己的类加载器来完成加载。注意：加载阶段与连接阶段的部分内容（如一部分字节码文件格式验证动作）是交叉进行的，加载阶段尚未完成，连接阶段可能就开始了，但这些夹在加载阶段之中进行的动作，仍然属于连接阶段的内容，这两个阶段的开始时间仍然保持着固定的先后顺序2、验证​ 该阶段主要包括以下4个阶段，目的是为了验证Class文件的二进制内容对于虚拟机来说是否合法、安全。文件格式验证，确保字节流符合Class文件格式的规范并且能被当前版本的JVM处理。例如：验证魔数、验证主次版本号、检查常量tag标志等等元数据验证，主要进行语义校验，确保字节码的语义描述符合《Java语言规范》。例如：该类是否有父类、是否错误继承了final类、是否实现了其父类或接口要求的所有方法等等字节码验证，主要通过数据流和控制流分析，确保程序语义合法且符合逻辑，以及确保类的方法体在运行时不会做出危害JVM安全的行为。例如：验证类型转换是否合法、保证任意时刻栈帧的数据类型与指令代码序列都能配合工作（例如不会出现类似于“在栈帧放置了个int类型的数据，使用时却按照long类型来加载到本地变量表里”）等等符号引用验证，发生于虚拟机将符号引用转化为直接引用的时候（转换动作发生在解析阶段）。例如：验证引用的类是否存在，验证引用的成员变量、类、方法是否能被当前类访问等等。​ 该验证的主要目的是为了确保解析行为能够正常执行，若无法通过符号引用验证，可能会抛出java.lang.IncompatibleClassChangeError异常的子类，如java.lang.IllegalAccessError、java.lang.NoSuchFieldError、java.lang.NoSuchMethodError等3、准备该阶段会正式为类成员变量（即static成员变量）分配内存，并将它们的初始值设置为默认值1public static int a = 123;例如上述的类变量定义，在准备阶段后，a在内存中的值仍然为0，赋予123这个行为是在初始化阶段才执行的数据类型零值数据类型零值int0booleanfalselong0Lfloat0.0fshort(short)0double0.0dchar‘\u0000’referencenullbyte(byte)0上面该表格为Java基本数据类型的零值，不过只要类字段的字段属性表中存在ConstantValue属性，那即使在准备阶段，该类变量也能初始化为指定的值1public static final int a = 123;例如这次的a被final修饰符所修饰，变成一个常量，那在准备阶段完成时，它就不是0而是123了4、解析&emsp;&emsp;虚拟机会将常量池内的符号引用替换为直接引用，该解析主要针对的是类或接口、字段、类方法、接口方法、方法类型、方法句柄和调用点限定符7类符号引用。​ 虚拟机规范之中并没有规定解析阶段的具体发生时间，也就是说，在转换成直接引用后，可以触发校验阶段的符号引用验证，从这也能看出来类加载的各个阶段在实际进行时可能是交错执行的5、初始化​ 初始化阶段会开始在内存中构造一个Class对象来表示该类，即执行类构造器()方法的过程需注意的是，()方法跟()方法并不相同()方法中执行的是对所有类变量的赋值动作和静态语句块(static{}块)中的语句合并操作，并且编译器收集的顺序是由语句在源文件中出现的顺序决定的虚拟机会确保先执行父类的()方法，因此第一个被执行的类肯定是java.lang.Object，并且父类中定义的静态语句块要优先于子类的变量赋值操作如果一个类中没有static语句块，也没有对类变量的赋值操作，那该类就不会生成()方法接口中不能使用静态代码块，但仍然有变量初始化的赋值操作，因此接口与类一样都会生成()方法，但接口与类不同的是，执行接口的()方法不需要先执行父接口的()方法，因为只有当父接口中定义的变量被使用时，父接口才会被初始化。此外，接口的实现类在初始化时也一样不会执行接口的()方法。虚拟机会保证()方法的执行在多线程中是安全的，如果多个线程去同时初始化一个类，那么只有一个线程会去执行该类的()方法，其他线程都要阻塞等待四、Java类加载器1、含义​ 类加载器（class loader）是用来将Java类加载到虚拟机中的代码模块，加载一个Class文件首先需要获取该Class的二进制字节流，这个过程可以通过自定义类加载器来实现，从而可以通过多种灵活的途径获取Class的二进制字节流。2、确定唯一性​ 每个加载器都有各自独立的类名称空间，一个类的唯一性要通过其类加载器来确定。判断两个类是否相等，只有这两个类是由同一个类加载器加载的前提下才有意义，否则，即使两个类来自于同一个Class文件，被同一个虚拟机加载，只要加载它们的类加载器不同，那这两个类就不相同3、类加载器的主要类型启动类加载器（Bootstrap ClassLoader）该加载器使用C++语言实现，是虚拟机自身的一部分，主要作用是加载&lt;JAVA_HOME&gt;\lib目录中的系统类库（如.rt.jar）扩展类加载器（Extension ClassLoader）该加载器由sun.misc.Launcher$ExtClassLoader实现，负责加载&lt;JAVA_HOME&gt;\lib\ext目录中的，或者被java.ext.dirs系统变量所指定的路径中的所有类库应用程序类加载器（Application ClassLoader）该加载器由sun.misc.Launcher$AppClassLoader实现，负责加载用户类路径（ClassPath）上所指定的类库，若应用程序没有自定义过自己的类加载器，一般情况下这个就是程序中默认的类加载器4、双亲委派模型图5-2 双亲委派模型​ 双亲委派模型要求除了顶层的启动类加载器外，其余的类加载器都应该有自己的父类加载器。这里类加载器之间的父子关系一般不会以继承的关系来实现，而是用组合关系来复用父类加载器的代码​ 每个类加载器都有自己的加载缓存，当一个类被加载后就会放入缓存，等下次加载时会直接从缓存中返回。​ 工作流程：当一个类加载器去加载一个Class时，它会先委派自己的父类加载器去加载，每层都是这样，因此所有的加载器请求最终都应该传到顶层的启动类加载器中，只有父类加载器无法加载时，子类才会尝试自己去加载，并将其放进自己的缓存中。​ 因此，加载过程可以看成自底向上检查类是否已经加载，然后自顶向下加载类，如下图所示。图5-3 双亲委派模型的加载过程​ 好处：双亲委派模型可以保证重要的系统Class始终由启动类加载器加载，确保Java运行环境的稳定性和正确性，避免用户自己编写的类动态替换 Java 的一些核心类（比如 String类）同时也避免了重复加载，因为 JVM 中区分不同类，不仅仅是根据类名，相同的 class 文件被不同的 ClassLoader 加载就是不同的两个类，如果相互转型的话会抛java.lang.ClassCaseException参考自：https://www.cnblogs.com/jqctop1/https://www.cnblogs.com/bokeyuanlongbin/p/9071629.html《深入理解Java虚拟机》]]></content>
      <tags>
        <tag>Java</tag>
        <tag>JVM</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[四、class文件的文件头]]></title>
    <url>%2F2019%2F09%2F16%2Fjvm-4%2F</url>
    <content type="text"><![CDATA[1、魔数（Magic Number）​ 前4个字节被称之为魔数，用于确定该文件是否为一个能被虚拟机接受的class文件​ class文件的魔数为0xCAFEBABE（咖啡宝贝）2、文件版本（Version）​ 第5、6个字节为次版本号，第7、8个字节为主版本号​ 下图为class文件版本号3、常量池（Constant Pool）​ 常量池入口，常量池主要存放字面量和符号引用​ 字面量：比较接近于Java语言层面中的常量概念，如文本字符串、申明为final的常量值等​ 符号引用：比较接近于编译原理方面的概念，主要包括类和接口的全限定名、字段的名称与描述符、方法的名称和描述符​ 常量池分析：https://blog.csdn.net/gcw1024/article/details/510268404、访问标志（Access Flags）​ 紧接着的两个字节表示访问标志，例如该class文件是类还是接口、是否是public、是否是abstract等等5、索引（Index）​ 类索引、父类索引、接口索引集合，三个都是u2（2个字节的无符号数）类型，这三项数据共同确定一个类的继承关系6、字段表集合（Field Info）​ 字段表，用于描述接口或者类中声明的变量，例如作用域（public、private……）、是实例变量还是类变量（static）、可变性（final）、并发可见性（volatile）、字段数据类型（基本类型、对象、数组）。​ 字段包括类级变量以及实例级变量，但不包括方法内部声明的局部变量7、方法表集合​ Class文件存储格式中对方法的描述和字段表几乎一致​ 在Java语言中，要重载一个方法，除了要跟原方法具有相同的简单名称外，还必须要有一个与原方法不同的特征签名，特征签名就是一个方法中各个参数在常量池中的字段符号的集合，也就是因为返回值不会包含在特征签名中，因此Java语言里面是无法仅仅依靠返回值的不同来进行方法重载。​ 但在Class文件中，特征签名的范围要更大些，只要描述符不是完全一致就可以共存，就是说如果两个方法有相同的名称和特征签名，但返回值不同，那么它们还是可以合法共存于同一Class文件当中8、属性表集合​ 在Class文件、字段表、方发表都可以携带自己的属性表集合]]></content>
      <tags>
        <tag>Java</tag>
        <tag>JVM</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[三、Java垃圾收集器和内存分配策略]]></title>
    <url>%2F2019%2F09%2F15%2Fjvm-3%2F</url>
    <content type="text"><![CDATA[一、回收机制算法1、引用计数算法&emsp;&emsp;概念：给对象添加一个引用计数器，每当对象被引用一次，计数器就加1， 当引用失效时，计数器值减1，当计数器为0时，则表示该对象可以被回收&emsp;&emsp;结果：没被主流采用，因为两个对象相互引用的话，会造成无法回收的死循环2、可达性算法​ 概念：从GC Roots作为起点开始搜索，当一个对象到GC Roots没有任何引用链相连，即该对象到GC Roots不可达时，该对象便可以被GC回收。​ 所谓“GC roots”，或者说tracing GC的“根集合”，就是一组必须活跃的引用。​ GC Roots的对象：​ ① 虚拟机栈（栈帧中的本地变量表）中引用的对象，例如各个线程被调用的方法堆栈中使用到的参数、局部变量、临时变量等​ ② 方法区中静态属性引用的对象，例如Java类的引用类型静态变量​ ③ 方法区中常量引用的对象，例如字符串常量池里的引用​ ④ 本地方法栈中JNI（即一般说的Native方法）引用的对象​ ⑤ JVM内部的引用，例如基本数据类型对应的Class对象、一些常驻的异常对象（例如NullPotinException、OutOfMemoryError）等，还有系统类加载器​ ⑥ 所有被同步锁（synchronized关键字）持有的对象​ ⑦ 把反应JVM内部情况的JMXBean、JVMTI中注册的回调、本地代码缓存等​ 除了这些固定的GC Roots集合外，根据用户所选用的垃圾收集器以及当前回收的内存区域不同，还可以有其他对象临时性地加入，共同构成完整GC Roots集合。GC Roots详解：https://www.zhihu.com/question/53613423/answer/135743258二、Java的四种引用强引用：类似于A a = new A()这类引用，只要强引用还存在，GC永远不会回收这类对象的内存软引用：系统将要发生内存溢出异常之前，GC会将这类对象回收弱引用：每当GC工作时，无论当前内存是否足够，这类对象都会被回收虚引用：最弱的引用，和没有引用差不多，并且随时可能被回收。为一个对象设置虚引用关联的唯一目的就是能在这个对象被GC回收时收到一个系统通知JDK1.2版之后提供了SoftReference类来实现软引用、WeakReference类实现弱引用、PhantomReference类实现虚引用三、存活依据进行可达性算法后发现并没有与GC Roots相连接的引用链，会被第一次标记并进行一次筛选若对象没有覆盖finalize()方法或者finalize()方法已被虚拟机调用过，则会被视为“没有必要执行”若该对象被判断为有必要执行finalize()方法，则该对象会被放置于名为F-Queue（即将回收）的队列中，等待执行finalize()方法稍后GC会对F-Queue队列中的对象进行第二次小规模标记，倘若对象没在finalize()方法中重新连上GC Roots的引用链，那它就会被回收注意：任何一个对象的finalize()方法都只会被调用一次，第一次要被回收时系统会去自动调用，可以看成是一次自救四、垃圾收集算法1、标记-清除算法​ 首先标记出所有需要回收的对象，标记完成后再统一回收所有被标记的对象​ 它是最基础的收集算法，主要不足有两个，一是标记和清理的效率都不高，另一个是会产生大量不连续的内存碎片2、复制算法​ 将内存分为大小相等的两部分，每次只使用其中一部分，倘若将这两部分看做A和B，则当A内存用完时，就会将存活对象复制到B内存上，再把A内存一次性全部清理掉。​ 优点：每次都是对整个半区进行内存回收，不会出现内存碎片的情况​ 缺点：可用内存只剩下原来的一半，代价太高了现在的商用JVM大多都优先采用这种收集算法去回收新生代，而由于新生代中的对象在一般场景下有98%熬不过第一轮收集，因此并不需要按照1：1的比例来划分新生代的内存空间3、标记-整理算法​ 标记过程跟“标记-清除算法”一样，但标记后会将所有存活对象向着同一端移动，然后直接清理掉端边界以外的内存HotSpot虚拟机中关注吞吐量的Parallel、Scavenge收集器是基于标记-整理算法的，而关注延迟的CMS收集器则是基于标记-清除算法的4、分代算法&emsp;&emsp;根据对象存活周期的不同将内存分为好几块，一般是将Java堆分为新生代和年老代，再根据各自年代的特点去使用不同的收集算法五、垃圾收集器​ 衡量垃圾收集器的三项最重要的指标是：内存占用（Footprint）、吞吐量（Throughput）和延迟（Latency），三者共同构成一个“不可能三角”。1、Serial收集器​ 新生代GC，单线程，采用复制算法，进行垃圾收集时会“Stop The World”。​ 下图为Serial/Serial Old收集器运行示意图2、ParNew收集器​ 新生代GC，Serial的多线程版本，一样采用复制算法，一样会STW​ 下图为ParNew/Serial Old收集器运行示意图3、Parallel Scavenge​ 新生代GC，多线程，采用标记-复制算法，它的目标是达成一个可控的吞吐量​ 下图为Parallel Scavenge/Parallel Old收集器运行示意图4、Serial Old收集器​ 老年代GC，单线程，采用标记-整理算法，可作为CMS收集器的后备预案5、Parallel Old收集器​ 老年代GC，多线程，采用标记-整理算法，JDK1.6时才开始提供，用来替代Serial Old6、CMS收集器​ 老年代GC，并发收集，采用标记-清除算法，以获取最短停顿为目的，但需要更多内存。​ CMS运行过程有以下四个步骤初始标记：仅仅标记下GC Roots能直接关联到的对象，速度很快，需要STW并发标记：进行GC Roots Tracing的过程，整个CMS处理过程中耗时最长重新标记：为了修正并发标记期间因用户程序继续运行而导致标记变动的那一部分对象的标记记录，这个阶段的停顿时间通常会比初始标记阶段长一些，但也远比并发标记阶段的时间短，需要STW并发清除：清理删除掉标记阶段判断的已死对象，不需要移动存活对象，可与用户线程同时并发。​ 下图为CMS收集器运行示意图7、G1收集器​ 老年代+新生代GC，能并行与并发，采用分代收集算法，可实现可预测的停顿。​ 它是面向堆内存任何部分来组成回收集（Collection Set，一般简称CSet）进行回收，其衡量标准不再是它属于哪个分代，而是哪块内存中存放的垃圾数量最多、回收收益最大，这就是G1收集器的Mixed GC模式。​ 其运行过程有以下四个步骤：初始标记：仅仅标记下GC Roots能直接关联到的对象，速度很快，需要STW并发标记：进行GC Roots Tracing的过程，递归扫描整个堆里的对象图，找出要回收的对象，并且扫描完成后还要重新处理SATB记录下的并发时有引用变动的对象。最终标记：对用户线程做一个短暂的暂停，用于处理并发阶段结束后仍遗留下来的最后那少量的SATB记录筛选回收：更新Region的统计数据，对各个Region的回收价值和成本进行排序，根据用户所期望的停顿时间来制定回收计划​ 除了并发标记外，其他三个阶段都需要STW​ 下图为G1收集器运行示意图关于GC收集器的详细内容可前往：https://www.cnblogs.com/cxxjohnson/p/8625713.html8、并发标记阶段如何保证收集线程与用户线程的互不干扰？​ CMS收集器采用增量更新算法来实现，而G1收集器则通过原始快照（SATB）算法来实现。​ STAB全称为snapshot-at-the-beginning，其目的是了维持并发GC的正确性。GC的正确性是保证存活的对象不被回收，换句话来说就是保证回收的都是垃圾。六、内存分配和回收策略1. 对象优先在新生代的Eden区分配​ 当Eden区没有足够空间进行分配时，JVM会发起一次Minor GC。2. 大对象会直接进入老年代&emsp;&emsp;-XX:PretenureSizeThreshold（可设置大于该参数的直接分配到老年代）3. 长期存活对象将进入老年代​ 对于Survivor区中的对象，每经历过一次Minor GC，相应年龄就会增加一岁​ -XX:MaxTenuringThreshold（一般默认为15，大于该参数的会被晋升到老年代）4. 动态对象年龄判定&emsp;&emsp;其实不一定要年龄大于上面那个参数才能晋升老年代，只要在Survivor空间中相同年龄所有对象大小的总和大于Survivor空间的一半，那么大于或等于该年龄的对象都能直接进入老年代5. 空间分配担保&emsp;&emsp;在发生Minor GC之前，虚拟机会先检查老年代最大可用的连续空间是否大于新生代所有对象大小总和，若该条件成立，才能放心去Minor GC，若不成立，则虚拟机会查看HandlePromotionFailure设置值是否允许担保失败，若允许，则会继续检查老年代最大可用的连续空间是否大于历次晋升到老年代对象的平均大小，若大于，才尝试去进行一次Minor GC，并且该MinorGC还存在风险；倘若小于，或HandlePromotionFailure设置不允许冒险，那就应该改为进行一次Full GC（为了避免Full GC过多，一般该参数会设置为打开状态）&emsp;&emsp;在JDK 6 Update 24之后，简化为发生Minor GC前检查老年代最大可用的连续空间是否大于新生代所有对象总大小或者历次晋升的平均大小，若大于则Minor GC，否则就Full GC七、补充部分Minor GC、Major GC、Full GC之间的区别：https://www.cnblogs.com/tuhooo/p/7508503.htmlGC算法和内存分配策略详细：https://crowhawk.github.io/2017/08/10/jvm_2/#hotspot%E7%9A%84%E7%AE%97%E6%B3%95%E5%AE%9E%E7%8E%B0]]></content>
      <tags>
        <tag>Java</tag>
        <tag>JVM</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[二、Java对象的创建、内存布局和访问]]></title>
    <url>%2F2019%2F09%2F11%2Fjvm-2%2F</url>
    <content type="text"><![CDATA[对象的创建1.使用指令创建对象​ 创建对象有多种方法，最常见的便是用New关键字创建对象，除此之外还可以使用反射机制（Class类的newInstance、使用Constructor类的newInstance方法）、Clone方法、反序列化等方式创建对象。​ 由类加载器负责根据一个类的全限定名来读取此类的二进制字节流到JVM内部，并存储在运行时内存区的方法区，然后将其转换为一个与目标类型对应的java.lang.Class对象实例。​12345678910111213141516171819202122232425262728293031323334353637383940414243public class Student implements Cloneable, Serializable &#123; private int id; public Student() &#123;&#125; public Student(Integer id) &#123;this.id = id;&#125; @Override protected Object clone() throws CloneNotSupportedException &#123; // TODO Auto-generated method stub return super.clone(); &#125; @Override public String toString() &#123;return "Student [id=" + id + "]";&#125; public static void main(String[] args) throws Exception &#123; // 使用new关键字创建对象 Student stu1 = new Student(123); // 使用Class类的newInstance方法创建对象 // 对应类必须具有无参构造方法，且只有这一种创建方式 Student stu2 = Student.class.newInstance(); // 使用Constructor类的newInstance方法创建对象 Constructor&lt;Student&gt; constructor = Student.class .getConstructor(Integer.class); // 调用有参构造方法 Student stu3 = constructor.newInstance(123); // 使用Clone方法创建对象 Student stu4 = (Student) stu3.clone(); // 使用(反)序列化机制创建对象 // 写对象 ObjectOutputStream output = new ObjectOutputStream( new FileOutputStream("student.bin")); output.writeObject(stu4); output.close(); // 读取对象 ObjectInputStream input = new ObjectInputStream(new FileInputStream( "student.bin")); Student stu5 = (Student) input.readObject(); &#125;&#125;2.类加载检查​ 根据new的参数检查能否在常量池中定位到一个类的符号引用（即类的带路径全名），并且检查这个符号引用代表的类是否已被加载、解析和初始化过，即该类有没被加载到方法区3.类加载​ 若找不到相应的符号引用，即该类没被加载到方法区，则先进行类加载，若该类已被加载过，则继续4.分配内存获取被加载类的对象所需内存大小（类加载后便可获知）检查是否在TLAB中分配内存，若是则在TLAB中分配内存，否则就在Eden中分配内存（少数情况下也可能会直接分配给老年代中，例如大对象就直接进入老年代）5.初始化零值​ 将分配到的空间全部初始化为零值（不包括对象头），例如int=0 string=null boolean=false，目前用户设定的初始值还未执行6.设置对象头信息​ 初始化零值完成之后，虚拟机要对对象进行必要的设置，例如这个对象是那个类的实例、如何才能找到类的元数据信息、对象的哈希码、对象的 GC 分代年龄等信息，这些信息存放在对象头（Object Header）中。另外，根据虚拟机当前运行状态的不同，如是否启用偏向锁等，对象头会有不同的设置方式。7.执行init方法​ 执行完上面的步骤之后，从虚拟机来看已经创建成功了，但对于Java程序而言还未真正完成，还需要调用init方法，把对象按照程序员的意愿去分配初始值，这样一个真正可用的对象才算创建完成如果对象是通过 clone() 方法创建的，那么 JVM 把原来被克隆的对象的实例变量的值拷贝到新对象中；如果对象是通过 ObjectInputStream 类的 readObject() 方法创建的，那么 JVM 通过从输入流中读入的序列化数据来初始化那些非暂时性(non-transient)的实例变量；如果实例变量在声明时被显式初始化，那么就把初始化值赋给实例变量，接着再执行构造方法。这是最常见的初始化对象的方式。对于内存分配的两种方式图2-1 内存分配的两种方式对于内存分配的并发问题两种保障方式​ 在创建对象的时候有一个很重要的问题，就是线程安全，因为在实际开发过程中，创建对象是很频繁的事情，作为虚拟机来说，必须要保证线程是安全的，通常来讲，虚拟机采用两种方式来保证线程安全：1、CAS+失败重试CAS是乐观锁的一种实现方式。所谓乐观锁就是，每次不加锁而是假设没有冲突而去完成某项操作，如果因为冲突失败就重试，直到成功为止。虚拟机采用 CAS 配上失败重试的方式保证更新操作的原子性。2、TLAB为每一个线程预先在 Eden 区分配一块儿内存，JVM 在给线程中的对象分配内存时，首先在 TLAB 分配，当对象大于 TLAB 中的剩余内存或 TLAB 的内存已用尽时，再采用上述的 CAS 进行内存分配3、关于TLAB的补充说明&emsp;&emsp;JVM在内存新生代Eden Space中开辟了一小块线程私有的区域，称作TLAB（Thread-local allocation buffer，线程本地分配缓存区）。默认设定为占用Eden Space的1%。在Java程序中很多对象都是小对象且用过即丢，它们不存在线程共享也适合被快速GC，所以对于小对象通常JVM会优先分配在TLAB上，并且TLAB上的分配由于是线程私有所以没有锁开销。因此在实践中分配多个小对象的效率通常比分配一个大对象的效率要高。&emsp;&emsp;也就是说，Java中每个线程都会有自己的缓冲区称作TLAB（Thread-local allocation buffer），每个TLAB都只有一个线程可以操作，TLAB结合bump-the-pointer技术可以实现快速的对象分配，而不需要任何的锁进行同步，也就是说，在对象分配的时候不用锁住整个堆，而只需要在自己的缓冲区分配即可。对象的内存布局1.对象头​ Hotspot 虚拟机的对象头包括两部分信息，第一部分用于存储对象自身的自身运行时数据（哈希码、GC 分代年龄、锁状态标志、线程持有的锁、偏向线程ID、偏向时间戳等等），这部分数据称为“Mark Word“；另一部分是Class对象的类型指针（Klass Pointer），即对象指向它的类元数据的指针，虚拟机通过这个指针来确定这个对象是哪个类的实例。图2-2 64位JVM对象结构描述2.实例数据&emsp;&emsp;实例数据部分是对象真正存储的有效信息，也是在程序中所定义的各种类型的字段内容。3.对齐填充&emsp;&emsp;对齐填充部分不是必然存在的，也没有什么特别的含义，仅仅起占位作用。 因为 Hotspot 虚拟机的自动内存管理系统要求对象起始地址必须是8字节的整数倍，换句话说就是对象的大小必须是8字节的整数倍。而对象头部分正好是8字节的倍数（1倍或2倍），因此，当对象实例数据部分没有对齐时，就需要通过对齐填充来补全。对象的访问定位&emsp;&emsp;建立对象就是为了使用对象，我们的 Java 程序通过栈上的 reference 数据来操作堆上的具体对象。对象的访问方式有虚拟机实现而定，目前主流的访问方式有两种。1.使用句柄​ 如果使用句柄的话，那么 Java 堆中将会划分出一块内存来作为句柄池，reference 中存储的就是对象的句柄地址，而句柄中包含了对象实例数据与类型数据各自的具体地址信息​ 使用句柄的好处：reference 中存储的是稳定的句柄地址，在对象被移动时（GC时很普遍会移动对象）只会改变句柄中的实例数据指针，而 reference 本身不需要修改2.使用直接指针​ 如果使用直接指针访问，那么 Java 堆对象的布局中就必须考虑如何放置访问类型数据的相关信息，而栈帧中的 reference 存储的直接就是对象的地址​ 使用直接指针的好处：访问速度快，它节省了一次指针定位的时间开销。​ HotSpot虚拟机主要就是使用这种方式进行对象访问，但从整个软件开发的范围来看，在各种语言、框架中使用句柄访问的情况也十分常见。]]></content>
      <tags>
        <tag>Java</tag>
        <tag>JVM</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[一、Java内存区域]]></title>
    <url>%2F2019%2F09%2F10%2Fjvm-1%2F</url>
    <content type="text"><![CDATA[运行时数据区域图1-1 运行时数据区域Java堆Java堆是JVM所管理的内存中最大的一块，属于线程共享的内存该内存的唯一目的就是存放对象实例，几乎所有的对象实例和数组都要在堆上分配内存Java堆是垃圾收集器管理的主要区域，有时候也被称之为GC堆（Garbage Collected Heap）。大部分GC收集器是基于分代收集理论设计的，例如我们认为的Java堆分为新生代、老年代、永久代、Eden区、From、Survivor区、To Survivor区，但实际上这并非《Java虚拟机规范》中对Java堆的细致划分，只是一部分垃圾收集器的设计风格将Java堆进一步细致划分只是为了更好地回收内存、更快地分配内存。在JDK9以后的G1收集器、CMS、ZGC等都不再采用分代收集设计模式。（不过对于目前主流的JDK8而言，还是采用的这种模式）如果在堆中没由内存完成实例分配，并且堆也无法扩展时，会抛出OutOfMemoryError异常方法区用于存储已被虚拟机加载的类信息、常量、静态变量、即时编译器（JIT）编译后的代码等数据，属于线程共享的内存它与”永久代“并不等价，方法区是一种规范、标准，而永久代只是HotSpot垃圾收集器的一种实现，在JDK1.8后，永久代被从方法区中去掉，取而代之的是元空间，即JDK1.8方法区的具体实现方式是元数据。永久代中的 class metadata 转移到了 native memory（本地内存，而不是虚拟机）；永久代中的 interned Strings 和 class static variables 转移到了 Java heap；永久代参数 （PermSize MaxPermSize） -&gt; 元空间参数（MetaspaceSize MaxMetaspaceSize）在主流版本的JDK8中，字符串常量池、静态变量放在堆中，类的元数据则存放在基于本机物理内存的元空间中当方法区无法满足内存分配时，会抛出OutOfMemoryError异常运行时常量池运行时常量池是方法区的一部分，属于线程共享的内存Class文件常量池用于存放编译期间生成的各种字面量和符号引用，它会在类加载后进入方法区的运行时常量池中存放，一个类加载对应一个运行时常量池运行时常量池相对于Class文件常量池来说具备动态性，除了预置的内容，在运行期间产生的新常量也能放入池中作为方法区的一部分，当常量池无法再申请到内存时会报OutOfMemoryError异常补充：https://blog.csdn.net/qq_41155436/article/details/106466770程序计数器程序计数器是一块较小的内存空间，可看作是当前线程所执行的字节码的行号指示器，为线程切换提供保障，属于线程私有的内存若线程正在执行一个Java方法，则计数器会记录正在执行的虚拟机字节码指令的地址，若执行的是Native方法，则该计数器为空（Undefined）该区域是Java虚拟机规范中唯一一个没有规定任何OutOfMemoryError情况的区域，因为它只存储和维护下一个需执行的命令的地址，即使是死循环也不会让内存超限程序计数器与线程调度：https://blog.csdn.net/leaf_0303/article/details/78953669Java虚拟机栈虚拟机栈描述的是Java方法执行的内存模型，生命周期与线程相同，属于线程私有的内存栈帧(Stack Frame)用于存储局部变量表、操作数栈、动态链接、方法出口等信息，每个方法从调用到执行完成的过程，都对应着一个栈帧在虚拟机栈中由入栈到出栈的过程局部变量表存放了编译期可知的各种基本数据类型、对象引用和returnAddress(下一条字节码指令地址)类型，所需空间会在编译期间完成分配，以局部变量槽（Slot）来表示，除64位长度的long和double类型数据会占用两个变量槽外，其余数据类型只占用一个。局部变量表的大小取决于虚拟机实现的每个变量槽的大小。该区域规定了两种异常状况，如果线程请求的栈深度大于虚拟机所允许的深度，则抛出StackOverflowError异常；若虚拟机栈动态扩展（大部分JVM允许扩展，也可设置不扩展）时无法申请到足够的内存，则抛出OutOfMemoryError异常栈帧的详细分析：https://blog.csdn.net/u014296316/article/details/82668670本地方法栈本地方法栈于虚拟机栈所发挥的作用差不多，属于线程私有的内存虚拟机栈为虚拟机执行Java方法服务，本地方法栈为虚拟机用到的Native方法服务抛出异常于虚拟机栈一样，StackOverflowError异常和OutOfMemoryError异常《Java虚拟机规范》对本地方法栈中方法使用的语言、使用方式与数据结构并没有任何强制性要求，因此具体的虚拟机可根据需要自由实现它，例如Hot-Spot虚拟机就直接把本地方法栈和虚拟机栈合二为一。直接内存直接内存既不是虚拟机运行时数据区的一部分，也不是《Java虚拟机规范》中定义的内存区域，它是Java堆外内存在JDK1.4时引入的NIO类，就是使用Native函数直接分配堆外内存，然后通过一个存储在Java堆内的DirectByteBuffer对象作为这块内存的引用进行操作本机直接内存的分配不会受到Java堆大小的限制，但它还是内存，仍然会受到本机总内存以及处理器寻址空间的限制，当服务器管理员在配置虚拟机参数时粗心大意，使得各个内存区域总和大于物理内存限制，就会导致动态扩展时出现OutOfMemoryError异常]]></content>
      <tags>
        <tag>Java</tag>
        <tag>JVM</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[SpringBoot项目中关于maven依赖冲突的BUG处理(搞了一天的BUG)]]></title>
    <url>%2F2019%2F05%2F12%2FSpringBoot-1%2F</url>
    <content type="text"><![CDATA[第一个BUG首先是原初之罪的BUG，出现了找不到Bean的情况中间是怎么解决的我忘记了，还引出一个新建的maven module没有配置Sources Root、Test Sources Root等相应文件夹，后面这个通过Project Structure的Modules设置和Module自身的.imi文件设置就能解决第二个BUG解决了这些后，又TM引出了一个BUG无论都导入不了tk.mybatis.mapper.common.Mapper包搞了半天都没处理好的我，毅然决然地使用了暴力破除法，直接重建一个项目，再将这个项目的东西搬过去，然后就好了……本以为这样终于结束了，因为到这里已经花掉我半天时间了——但！是！第三个BUG这里又双叒叕冒出来一个BUG了查看了下出错的源代码，很明显的NoSuchMethodError，找不到对应方法，源代码也是干脆利落地给出了红色标注我当场就懵了，总不会是源代码自己出错吧，没理由啊，肯定是自己太菜了，哪里弄错了才对于是我又继续埋头苦干，边自己捣鼓，边去群里询问期间从某个学习群的群友那里得到了一个可能是导错包了的原因，然后我想错了，以为是项目对于maven仓库包的引用导入有问题，需要更新下什么的，就把整个maven本地仓库删掉再重加载，再把项目的Libraries引用全删了再重新用mvn compile指令加载回来然后不出意外的——问题并没有得到解决（理所当然的，因为问题根本就不是出在那里）最后发现是pom.xml配置文件里面的依赖冲突导致的，将其那段多余的冲突依赖删掉后就没事了处理依赖冲突的maven helper插件最后有个群友推荐了IDEA的一个专门处理maven依赖冲突的插件，maven helper，下载好了后重启，即可使用重启后进入pom.xml配置文件内，可看到以下内容使用时，右键可以将用不到的版本Exclude掉，不过要解决问题大多没那么简单直接Exclude就能完事的，相关教程什么的可以去网上找，这里就不多说了]]></content>
      <tags>
        <tag>springboot</tag>
        <tag>bug</tag>
        <tag>maven</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Next主题从5.1版本升级到7.1的捣鼓]]></title>
    <url>%2F2019%2F04%2F30%2Fhexo-next-1%2F</url>
    <content type="text"><![CDATA[准备下载Next最新主题并将解压到themes目录下，旧版本的Next主题先保留着作个数据对照和参考，等到新版本完全配置完毕，再删了也不迟，下面我只简单说些我感受到的版本差异，还有一些美化优化设置设置头像我用5.1版本时，头像的设置要在hexo的主配置 _config.yml 文件下添加以下代码1avatar: 头像图片的url地址新版本已经将头像设置的功能集合到Next主题的 _config.yml 配置文件中，我将头像图片放在hexo/source/imges中，配置如下图所示设置网站logo该功能同样集合在Next主题的 _config.yml 配置文件中，将分割好的网站logo文件替换掉Next主题的images文件夹下的logo文件，总共有三个文件要替换，注意大小跟格式要匹配Next主题的 _config.yml 配置文件，搜关键词 favicon 即可找到对应网站logo设置设置阅读全文在个人博客中，倘若文章显示全文，那在首页就长得没法看后面的文章了，所以我们要给他设置个预览效果在Next主题的_config.yml配置文件中，搜索 auto_excerpt 并将其唤醒，enable的参数为是否唤醒，length的参数则为展示的高度设置搜索功能安装插件1npm install hexo-generator-searchdb --save设置hexo的主配置文件在文件中添加以下代码123456# 本地搜索search: path: search.xml field: post format: html limit: 10000设置Next主题的配置文件配置文件中有集成该搜索功能，搜索 local_search 并将其enble设置为true即可启动12345local_search: enable: true trigger: auto top_n_per_article: 1 unescape: false设置背景图片以及界面透明化美化修改Next主题下的source/css/_custom中的 custom.styl 文件，注意此处的背景图片是放在hexo根目录下的source/images/不过用上述这种方法将透明度修改过后，会造成一个BUG——搜索框不再置顶，而是被其他模块挡住了这个我折腾了半天，想出了个简单粗暴的方法，直接在设置透明度时给它来个z-index:9999的属性，虽说这样能解决问题，但解决BUG的代码是引出另外的、不怎么明显的BUG从上图可以看出，菜单栏与搜索框一同置顶了，所以高亮的颜色跟周围有所不同，并且搜索框也被透明化了，让人观看时的体验不是很舒服不过在这时，我不久前求助的群里，有个群友说他曾经也遇到过这个透明化问题，但他的解决方法是将菜单栏单独设置成白色透明的背景色，这样就能避免跟搜索框产生冲突，我去试了一下，这个方法能完美解决上述问题，同时不会让搜索框透明化]]></content>
      <tags>
        <tag>hexo</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[阿里云Centos7.4服务器配置jdk、tomcat、mysql教程]]></title>
    <url>%2F2019%2F04%2F02%2FaliCloud_1%2F</url>
    <content type="text"><![CDATA[一.配置jdk1.8这个没啥好说的，直接输入指令安装1yum -y install java-1.8.0-openjdk.x86_64安装成功后会有Complete提示，并且可通过java -version查看当前版本二.搭建tomcat8用xftp工具与服务器连接后，将下载好的tomcat压缩包丢到/usr/local/目录下，再使用Xshell工具，输入命令1tar -zxvf apache-tomcat-8.5.39.tar.gz解压到当前目录，并将文件名改为tomcat8，之后就能将压缩包删了阿里云服务器设置开放8080端口防火墙开放8080端口12开放端口：firewall-cmd --zone-public --add-port=8080/tcp --permanent重新加载：firewall-cmd --reload启动tomcat测试提高阿里云服务器部署的tomcat外界访问速度，以下为原文地址https://blog.csdn.net/qq_40386113/article/details/84837881三.mysql安装配置执行命令, 安装mysql1yum -y install mysql-community-server检查是否安装成功设置开机自动启动mysql，使用以下两个命令12systemctl enable mysqldsystemctl list-unit-files | grep mysqld查看是否设置成功启动mysql服务，并查看mysql服务是否已启动12systemctl start mysqldps -ef|grep mysql进行mysql的用户名、密码等信息进行配置1mysql_secure_installation启动mysql1mysql -uroot -p防火墙开放3306端口12firewall-cmd --zone-public --add-port=3306/tcp --permanentfirewall-cmd --reload配置远程登录1grant all privileges on *.* to &apos;root&apos;@&apos;%&apos; identified by &apos;root&apos; with grant option;（指令意为将所有权限赋予给 root 用户，并允许其进行远程登录）红框部分填写的是访问远程访问root用户所使用的密码最后，用其他机子测试连接成功]]></content>
      <tags>
        <tag>阿里云Centos</tag>
      </tags>
  </entry>
</search>
